// src/setupTests.js
// Runs before every test file — imports the jest-dom custom matchers so
// assertions like toBeInTheDocument() and toHaveTextContent() work globally.
import "@testing-library/jest-dom";
